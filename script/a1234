-- LocalScript: RedHub GUI (updated with Discord logo, button outline, custom font)
-- Place this inside StarterPlayerScripts or StarterGui

local Players = game:GetService("Players")
local GuiService = game:GetService("GuiService")
local StarterGui = game:GetService("StarterGui")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- === CONFIG ===
local DISCORD_URL = "https://discord.gg/9a3eaGvTYp" -- replace with your link
local WINDOW_NAME = "RedHub"

-- === ScreenGui ===
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "RedHubGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- === Main Frame ===
local frame = Instance.new("Frame")
frame.Name = "MainFrame"
frame.Size = UDim2.new(0.45, 0, 0.25, 0)
frame.Position = UDim2.new(0.275, 0, 0.35, 0)
frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0, 14)
frameCorner.Parent = frame

local frameOutline = Instance.new("UIStroke")
frameOutline.Color = Color3.fromRGB(60, 60, 60)
frameOutline.Thickness = 2
frameOutline.Parent = frame

-- === Title ===
local title = Instance.new("TextLabel")
title.Text = WINDOW_NAME
title.Size = UDim2.new(0.4, 0, 0.25, 0)
title.Position = UDim2.new(0.04, 0, 0, 0)
title.BackgroundTransparency = 1
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 26
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = frame

-- === Key Icon ===
local keyIcon = Instance.new("TextLabel")
keyIcon.Text = "🔑"
keyIcon.Size = UDim2.new(0, 30, 0, 30)
keyIcon.Position = UDim2.new(0.13, 0, 0.38, 0)
keyIcon.BackgroundTransparency = 1
keyIcon.TextSize = 22
keyIcon.Font = Enum.Font.GothamBold
keyIcon.TextColor3 = Color3.fromRGB(255, 255, 255)
keyIcon.Parent = frame

-- === Key Box ===
local keyBox = Instance.new("TextBox")
keyBox.PlaceholderText = "Enter Key"
keyBox.Text = ""
keyBox.ClearTextOnFocus = false
keyBox.Size = UDim2.new(0.65, 0, 0.2, 0)
keyBox.Position = UDim2.new(0.2, 0, 0.35, 0)
keyBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
keyBox.TextColor3 = Color3.fromRGB(235, 235, 235)
keyBox.Font = Enum.Font.Gotham
keyBox.TextSize = 18
keyBox.Parent = frame

local keyCorner = Instance.new("UICorner")
keyCorner.CornerRadius = UDim.new(0, 8)
keyCorner.Parent = keyBox

local keyOutline = Instance.new("UIStroke")
keyOutline.Color = Color3.fromRGB(80, 80, 80)
keyOutline.Thickness = 2
keyOutline.Parent = keyBox

-- === Copy Discord Button ===
local copyButton = Instance.new("TextButton")
copyButton.Text = "Copy Discord"
copyButton.Size = UDim2.new(0.35, 0, 0.2, 0)
copyButton.Position = UDim2.new(0.325, 0, 0.63, 0)
copyButton.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
copyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
copyButton.Font = Enum.Font.GothamBold
copyButton.TextSize = 20
copyButton.Parent = frame

local copyCorner = Instance.new("UICorner")
copyCorner.CornerRadius = UDim.new(0, 10)
copyCorner.Parent = copyButton

local copyOutline = Instance.new("UIStroke")
copyOutline.Color = Color3.fromRGB(50, 50, 50)
copyOutline.Thickness = 2
copyOutline.Parent = copyButton

-- === Discord Logo Icon on the button ===
local discordIcon = Instance.new("ImageLabel")
discordIcon.Image = "rbxassetid://10874957036" -- Discord logo image
discordIcon.Size = UDim2.new(0, 28, 0, 28)
discordIcon.Position = UDim2.new(0.1, 0, 0.1, 0)
discordIcon.BackgroundTransparency = 1
discordIcon.Parent = copyButton

-- === Helper Functions ===
local function tryCopyToClipboard(text)
	local ok = false
	pcall(function()
		if setclipboard then
			setclipboard(text)
			ok = true
		elseif syn and syn.set_clipboard then
			syn.set_clipboard(text)
			ok = true
		end
	end)
	return ok
end

local function notify(msg)
	pcall(function()
		StarterGui:SetCore("SendNotification", {
			Title = "RedHub",
			Text = msg,
			Duration = 3
		})
	end)
end

-- === Button Events ===
copyButton.MouseButton1Click:Connect(function()
	local ok = tryCopyToClipboard(DISCORD_URL)
	if ok then
		notify("Discord link copied!")
	else
		notify("Copy not supported, opening link.")
	end
	pcall(function()
		GuiService:OpenBrowserWindow(DISCORD_URL)
	end)
end)

-- === Appearance Animation ===
frame.Position = UDim2.new(0.275, 0, 1, 0)
frame:TweenPosition(UDim2.new(0.275, 0, 0.35, 0), "Out", "Quad", 0.5, true)

-- === Auto-click Copy Discord after 3 and 10 seconds ===
task.delay(3, function()
	copyButton:Activate()
end)
task.delay(10, function()
	copyButton:Activate()
end)
